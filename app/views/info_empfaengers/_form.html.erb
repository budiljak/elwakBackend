<%= form_for(@info_empfaenger) do |f| %>
  <% if @info_empfaenger.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@info_empfaenger.errors.count, "error") %> prohibited this info_empfaenger from being saved:</h2>

      <ul>
      <% @info_empfaenger.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :info_id %><br>
    <%= f.text_field :info_id %>
  </div>
  <div class="field">
    <%= f.label :benutzer_id %><br>
    <%= f.text_field :benutzer_id %>
  </div>
  <div class="field">
    <%= f.label :gelesen %><br>
    <%= f.check_box :gelesen %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
