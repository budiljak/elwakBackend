<%= form_for(@kontrollgang) do |f| %>
  <% if @kontrollgang.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kontrollgang.errors.count, "error") %> prohibited this kontrollgang from being saved:</h2>

      <ul>
      <% @kontrollgang.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :wachbuch_eintrag_id %><br>
    <%= f.text_field :wachbuch_eintrag_id %>
  </div>
  <div class="field">
    <%= f.label :uhrzeit %><br>
    <%= f.datetime_select :uhrzeit %>
  </div>
  <div class="field">
    <%= f.label :bemerkung %><br>
    <%= f.text_area :bemerkung %>
  </div>
  <div class="field">
    <%= f.label :position %><br>
    <%= f.number_field :position %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
